services:
  hosuto:
    image: ghcr.io/tammaroivan/hosuto:latest
    ports:
      - "4678:4678"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${STACKS_DIR:-/opt/stacks}:/stacks
      - hosuto-data:/data
    restart: unless-stopped

volumes:
  hosuto-data:
